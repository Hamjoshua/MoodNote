<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/apk/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0D1117"
    android:fillViewport="true">

<LinearLayout
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:orientation="vertical"
android:padding="16dp">

<!-- Header (как в GitHub) -->
<TextView
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Emotion Dashboard"
    android:textColor="#E6EDF3"
    android:textSize="24sp"
    android:textStyle="bold" />

<!-- Календарь эмоций (как Contributions) -->
<TextView
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="16dp"
    android:text="Your Emotions This Month"
    android:textColor="#7D8590"
    android:textSize="16sp" />

<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="8dp"
    android:orientation="horizontal">

    <!-- Пример: GridLayout для календаря эмоций -->
    <GridView
        android:id="@+id/emotionCalendarGrid"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnWidth="24dp"
        android:gravity="center"
        android:horizontalSpacing="4dp"
        android:numColumns="7"
        android:stretchMode="columnWidth"
        android:verticalSpacing="4dp" />

</LinearLayout>

<!-- График эмоций (столбчатая диаграмма) -->
<TextView
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="24dp"
    android:text="Weekly Emotion Stats"
    android:textColor="#7D8590"
    android:textSize="16sp" />

<com.github.mikephil.charting.charts.BarChart
    android:id="@+id/emotionBarChart"
    android:layout_width="match_parent"
    android:layout_height="250dp"
    android:layout_marginTop="8dp" />

<!-- Список последних эмоций (как Activity) -->
<TextView
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="24dp"
    android:text="Recent Emotions"
    android:textColor="#7D8590"
    android:textSize="16sp" />

<androidx.recyclerview.widget.RecyclerView
    android:id="@+id/recentEmotionsList"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="8dp"
    android:orientation="vertical"
    app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

</LinearLayout>
    </ScrollView>